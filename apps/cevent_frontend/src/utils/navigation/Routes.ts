export const routes = {
  home: '/',
  myTickets: '/my_tickets',
  myEvents: '/my_events',
  profile: '/profile',
  createEvent: '/create_event',
  checkoutTicket: '/checkout'
};

export type Routes = typeof routes;
